{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build"],
      "passThroughEnv": ["TEST_BASE_URL", "TEST_ADMIN_PASSWORD"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
